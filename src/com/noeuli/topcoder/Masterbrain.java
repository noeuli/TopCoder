package com.noeuli.topcoder;

// SRM 146 Div 1 Level 2 - too hard to calculate.
public class Masterbrain {
	/**
	 * Given a String[] of guesses and a String[] of results for those guesses, 
	 * return the total number of possible secret combinations, assuming that 
	 * exactly one of the results is incorrect. Each element of results will be 
	 * formatted as "<x>b <y>w", where <x> and <y> are the number of black and 
	 * white pegs respectively.
	 * 
	 * @param guesses
	 * @param results
	 * @return
	 */
	public int possibleSecrets(String[] guesses, String[] results) {
		int result = 0;
		
		return result;
	}
}
